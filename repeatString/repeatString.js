const repeatString = function(i,words) {
      let sen;
      for(let count = 0; count <= i; count++) {
            if(i > 0) {
                  sen += words;
            } else {
                  return ("ERROR!")
      }
            return (sen)
}

module.exports = repeatString
